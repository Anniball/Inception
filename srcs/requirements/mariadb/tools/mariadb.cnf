# More configuration details for mariaDB
[galera] #We need it to precise address and port
bind-address=0.0.0.0
port=3306

#[client-server]

#!includedir /etc/mysql/conf.d
#!includedir /etc/mysql/mariadb.conf.d

#[mysqld]
#skip-networking=false
#skip-bind-address
